[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts =
    -q
    --strict-markers
    --tb=short
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning
markers =
    slow: 需要较长时间或较多网络请求的用例
